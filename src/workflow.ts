import { HOSTS } from './resources/hosts.js';
import { CATALOG } from './kb/catalog.js';
import { DOMAINS } from './kb/domains.js';

const workflow = `# Регламент работы с форумом yougame.biz

Ты работаешь с закрытым форумом разработчиков через MCP-сервер \`yougame\`.
Порядок ниже — рабочая рамка, а не догма: если пользователь просит просто «найди тему» —
просто найди тему. Полный цикл нужен, когда просят что-то **сделать** (написать код, собрать
чит/скрипт/мод, разобраться в механике).

## Шаг 0. Понять запрос
Определи, что нужно: факт (найти/прочитать), артефакт (скачать), или реализация (написать код).
От этого зависит, нужен ли ресерч.

## Шаг 1. Теоретический ресерч (гайды)
Цель — понять предметную область раньше, чем писать код.
1. \`yg_search\` по ключевым словам задачи, \`type: "thread"\` — заголовки тем.
2. Приоритет разделам с гайдами и туториалами («Полезные статьи и туториалы»,
   «Вопросы и помощь новичкам», «Общий раздел»).
3. \`yg_thread\` с \`firstPostOnly: true\` — статьи читаются быстро; поле \`aiSummary\` даёт
   краткое описание темы от самого форума, используй его для отсева.
4. Выпиши: термины, версии игры/движка/лоадера, ограничения, типичные ошибки.

## Шаг 2. Технический ресерч (код)
1. Ищи разделы с исходниками («Исходники читов …»), префиксы вида «Исходник», «Визуальная часть».
2. \`yg_thread\` целиком: блоки кода приходят отдельным полем \`code\` — это готовые примеры.
3. \`yg_resources\` — вложения и ссылки на архивы с исходниками; при необходимости \`yg_download\`.
4. Сверь несколько источников: на форуме много переносов и «недофиксов», один пример может быть сломан.

## Шаг 3. Реализация
1. Скажи пользователю, на чём основан вывод: 2–5 ссылок на конкретные темы.
2. Пиши код/решение сам, опираясь на найденное, а не копируй вслепую.
3. Явно отметь, чего в источниках не хватило и что ты додумал.

## Правила
- Комментарии под статьёй читай всегда: там правки, фиксы и «не работает на версии X».
- Прежде чем скачивать — покажи пользователю список найденных ресурсов.
- Не выдумывай ссылки и id тем: любой факт должен приходить из вызова инструмента.
- Форум русскоязычный, отвечай на языке пользователя.`;

const gates = `# Доступ, хайды и авторизация

Форум показывает часть контента только авторизованным. Признаки:

| Признак | Что это | Что делать |
|---|---|---|
| Ответ \`AUTH_REQUIRED\` от инструмента | закрытый раздел или тема (HTTP 403 → страница входа) | сказать пользователю и вызвать \`yg_login\` |
| В тексте поста \`🔒 [СКРЫТО: …]\` | хайд-блок аддона | \`yg_login\`, затем повторить \`yg_thread\` |
| \`🔒 [ССЫЛКА СКРЫТА ОТ ГОСТЕЙ]\` | ссылка спрятана от гостей | то же самое |
| \`gates[].kind = "reply"\` | контент открывается только после ответа в теме | **не отвечай за пользователя** — сообщи, что нужен его ответ в теме |
| \`gates[].kind = "posts" / "reaction"\` | нужен ранг: N сообщений или реакций | скажи пользователю честно: контент недостижим этим аккаунтом |
| \`gates[].kind = "user-specific"\` | «ответ с хайдом»: блок адресован конкретному нику | открыть невозможно ни входом, ни ответом — просто пропусти и скажи об этом |

\`yg_login\` открывает настоящее окно браузера со страницей входа: пароль, капчу и 2FA
вводит пользователь сам. Сервер ждёт появления куки xf_user и сохраняет сессию на диск,
после чего все инструменты работают от имени пользователя. Пароль ты не видишь и не запрашиваешь.

Если пользователь отказывается входить — продолжай работу с открытой частью форума и
скажи, чего именно не хватает.`;

const hostsTable = `# Файлообменники: как качать

\`yg_download\` определяет хостинг сам и идёт по цепочке
резолвер хоста → прямой GET → поиск ссылки в HTML → окно браузера.
Таблица нужна, когда автоматика не справилась и надо понять почему.

| Хостинг | Стратегия | Что делать |
|---|---|---|
${HOSTS.map((h) => `| ${h.name} | ${h.strategy} | ${h.instruction.replace(/\n/g, ' ')} |`).join('\n')}

Общие правила:
- Ссылки в постах обёрнуты в \`/proxy.php?link=…\` — сервер разворачивает их сам, настоящий адрес уже в поле \`url\`.
- Если хостинг требует капчу, ожидание или оплату — не обходи это, покажи ссылку пользователю.
- Скачанное складывается в каталог загрузок сервера; в ответе есть путь, размер и sha256.
- Архив с исходниками после скачивания не запускай — распаковывай и читай код.`;

const ladder = `# Лестница решений: что делать до того, как писать код

Правило одно: **сначала выясни, решено ли это до тебя, потом решай сам.**
Каждая ступень отвечает на вопрос «можно ли остановиться здесь». Спускайся вниз
только если ответ «нет».

## 0. Это вообще задача из мира yougame?
Чит, мод, дампер, инжектор, оффсеты, реверс, скрипт для чита, обход, античит, движок игры —
да. Обычный бэкенд, вёрстка, скрипт для работы — нет, работай как обычно, форум не трогай.
\`yg_plan\` отвечает на этот вопрос полем \`inScope\`.

## 1. Уже есть в локальной базе?
\`yg_kb_search\` — прошлые находки, факты и инструменты лежат на диске и переживают
перезапуск. Нашлось — переиспользуй и не ходи на форум второй раз.

## 2. Готовый инструмент уже существует?
\`yg_tools\` — встроенный каталог: дамперы, декомпиляторы, деобфускаторы, инжекторы,
генераторы SDK, маппинги. **Если задача закрывается существующим инструментом — не пиши свой.**
Объясни пользователю, чем пользоваться и как. Свой код нужен там, где готовое не подходит,
и это надо уметь объяснить.

## 3. Решение есть на форуме?
\`yg_search\` по запросам из плана: сначала теория (гайды и статьи), потом техника
(разделы «Исходники …», префикс «Исходник»). Тему читай целиком: **комментарии обязательны** —
там фиксы, «не работает на версии X» и предупреждения о нерабочих пастах.
Каждую находку клади в \`yg_note\` со ссылкой на тему.

## 4. Ресерч закрыт?
\`yg_ready\` — проверка гейта. Пока \`ready: false\`, код писать рано: в ответе лежит список
недостающего и как его добрать. Если форум по теме пуст — это нормальный исход:
зафиксируй \`yg_note kind="gap"\`, и гейт откроется.

## 5. Пиши минимум поверх найденного
Начни ответ с 2–5 ссылок на конкретные темы, потом решение. Не копируй пасту вслепую:
сверь минимум два источника. Явно скажи, чего в источниках не хватило и что ты додумал сам.

## 6. Верни знание в базу
\`yg_kb_add_tool\` на найденные инструменты, \`yg_report\` — итог сессии.
Следующая похожая задача начнётся с шага 1 и закончится быстрее.

## Чего гейт не делает
Не блокирует навсегда: у \`yg_ready\` есть \`force\` с обязательной причиной, и она
попадает в отчёт. Не заставляет входить на форум: если пользователь не хочет логиниться —
работай с открытой частью и скажи, чего не хватило. Не отвечает за пользователя в темах
ради хайда.`;

const toolbase = `# Встроенный каталог инструментов

Это стартовая база: что уже написано до тебя. Запрос — \`yg_tools\`
(фильтры \`domain\`, \`kind\`, \`query\`). Всё, что найдётся на форуме сверх этого,
дописывается через \`yg_kb_add_tool\` и попадает в ту же выдачу.

## Домены

| Домен | О чём | Разделы форума (теория / исходники) |
|---|---|---|
${DOMAINS.map((d) => `| \`${d.id}\` | ${d.title} | ${d.theoryForums.join(', ')} / ${d.codeForums.join(', ')} |`).join('\n')}

## Инструменты

| Инструмент | Вид | Домены | Что делает |
|---|---|---|---|
${CATALOG.map((t) => `| ${t.url ? `[${t.name}](${t.url})` : t.name} | ${t.kind} | ${t.domains.join(', ')} | ${t.summary} |`).join('\n')}

Виды: \`dumper\` — достаёт структуры и оффсеты, \`decompiler\` / \`disassembler\` — читают код,
\`deobfuscator\` — снимает защиту имён, \`injector\` / \`hook\` — внедрение и перехват,
\`loader\` — модлоадер, \`sdk-gen\` — генерирует заголовки под свой код, \`mapping\` — имена символов,
\`memory\` — работа с памятью процесса, \`analysis\` — разведка, \`render\` — интерфейс, \`build\` — тулчейн.`;

export const WORKFLOW = { workflow, gates, hostsTable, ladder, toolbase };
